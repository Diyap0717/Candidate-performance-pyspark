# Dataset

The original course project used `Student_performance_10k.csv`, described in the project report as a 10,000-record synthetic student-performance dataset.

The dataset file is not included here because it was not supplied with the notebook and report used to create this repository.

Expected filename:

```text
Student_performance_10k.csv
```

Expected fields include demographic attributes, preparation status, four subject scores, `total_score`, and `grade`.
